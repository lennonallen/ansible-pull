- hosts: localhost
  connection: local
  become: true

  vars_files:
    - vars/vault.yml

  roles:
    - packages
    - unattended-upgrades
    - role: ct105-packages
      when: ansible_hostname == "CT105"
